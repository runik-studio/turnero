version: "3.8"

services:
  api:
    build: .
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    environment:
      - FIRESTORE_PROJECT_ID=turnero-165d4
      - GOOGLE_APPLICATION_CREDENTIALS=/app/firebaseCredentials.json
      - MOCK_AUTH=false
